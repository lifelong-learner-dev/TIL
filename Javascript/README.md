# Javascript Part 1

| ```<script>``` | | | |
|:---:|:---:|:---|:---|
| 1   | array | ```// 1. Create an array with a size of 5 let num = new Array(5);``` <br> ```// Store values in each element of the array all at once for (let i = 0; i < num.length; i++) {num[i] = i;}``` <br> ```// Print the values of each element in the array using a loop``` <br> (for loop) <br> ```document.write("num: ");``` <br> ```for (let i = 0; i < num.length; i++) {document.write(num[i] + " ");}```  <br> <br>  ```// 2. Declare an array without a predefined size let player = new Array();```  <br> <br>  ```// Input values for (let i = 0; i < 3; i++) {player[i] = "Player" + (i + 1);}```  <br> <br>  ```// Output values document.write("<br> player: "); for (let i = 0; i < player.length; i++) {document.write(player[i] + " ");} ``` <br> <br>  ```// 3. Declare an array and initialize it with values (method 1) let fish = new Array("Mackerel", "Yellowtail", "Alaska Pollock", "Cod"); ```  <br> <br>  ```// Output values document.write("<br> fish: "); for (let i = 0; i < fish.length; i++) {document.write(fish[i] + " ");}```  <br> <br>  ```// 4. Declare an array and initialize it with values (method 2) let score = [85, 90, 75, 99]; ```  <br> <br>  ```// Output values document.write("<br> score: "); for (let i = 0; i < score.length; i++) {document.write(score[i] + " ");} ```  <br> <br>  ```// 5. When elements in the array have different data types let student = ["John Doe", 4, 93.5, 'A'] ``` <br> <br>  ```// Output values document.write("<br> student: "); for (let i = 0; i < student.length; i++) {document.write(student[i] + " ");} ```|  |
| 2  | break | let i = 0; <br> while (true) <br> { // Infinite loop <br> i += 3; <br> //Since it's an infinite loop, a condition is needed to exit the loop. <br> console.log(i); <br><br> if (i > 20) break; <br>// Exit the loop if i becomes greater than 20. <br> It terminates when i reaches 21. <br> ```document.write(i + "<br>");``` } <br><br> // (1) Ending only the inner for loop  <br>for (let i = 1; i <= 5; i++) { <br> for (let j = 1; j <= 3; j++)<br> { ```document.write(i + " : " + j + "<br>");``` <br> if (j == 2) {break; <br>// Terminate only the inner for loop; <br>the outer for loop continues executing <br> } <br> } <br>} <br> ```document.write("<br>");``` <br><br> // (1) Ending only the inner for loop using a label <br>Outter: for (let i = 1; i <= 5; i++) { <br> for (let j = 1; j <= 3; j++) { <br>  ```document.write(i + " : " + j + "<br>");``` <br>  if (j == 2) {break Outter;  <br>// Terminate only the inner for loop;  <br>the outer for loop continues executing } }}   |  |
| 3  | confirm  | // Using variables to store input values <br>// Variable usage method: Declare with 'let' and store a value <br><br>  let isStudent = confirm("Are you a student?");  <br>// [OK] / [Cancel] buttons <br><br>  // If the [OK] button is pressed,  <br>'true' is returned and stored in the 'isStudent' variable <br><br>  <br>// If the [Cancel] button is pressed,  <br>'false' is returned and stored in the 'isStudent' variable <br><br>  // If the returned value is 'true',  <br>it means the person is a student with a 50% discount <br><br>  // If 'false', <br>it means the person is not a student and there is no discount <br><br>  if (isStudent == true) { <br>    result = "You are a student."; <br>    discount = "50%"; <br>} else { <br>    result = "You are not a student."; <br>    discount = "None"; <br>} <br><br>  document.write("Result: " + result + "<br>"); <br>document.write("Discount: " + discount);|   |
| 4   | const  | // const rate;  <br><br>// Initialization must be done at the time of declaration;  <br>if not, it will result in an error <br><br> const rate = 0.07; <br>rate = 0.17;  <br><br>  // Constants cannot be changed once assigned a value <br>// const.html:11 Uncaught TypeError:  <br>Assignment to constant variable. <br><br>  console.log(rate); |  |
| 5 | continue  | for (let i = 1; i <= 10; i++) { <br> // If 'i' is an odd number, <br> skip statement 1 and continue to the next iteration. <br> // Result: Only even numbers are printed. <br><br> if (i % 2 == 1) { <br> continue; <br> } <br> ``` document.write(i + "<br>");``` // Statement 1 <br>} | This one will be used often. |
| 6  | dataType | let num1 = 15;  <br>// Integer <br><br>  let num2 = 123.45;  <br>// Floating-point number <br><br>  let answer = "y";  <br>// Character <br><br>let name = "John Doe";  <br>// String <br><br>let address = "1 Main Street, City";  <br>// String (single or double quotes can be used) <br><br>  let result = true; // Boolean value <br><br>  let nothing; // Variable declaration without a value <br><br>  // When [Cancel] is clicked, it returns a null value <br>let input = prompt("Data Type Example", "Click the Cancel button"); <br><br>  // 1. Numeric Operations <br>```document.write("<br> Integer * Floating-point: " + num1 * num2);``` <br><br>  // 2. Type Conversion: Floating-point to Integer (123.45 -> 123) <br>document.write( <br>```"<br> Convert Floating-point to Integer: " + parseInt(num2)``` ); <br><br>  // 3. Converting Numbers to Strings:  <br>15 + 123.45 = "15123.45" (String concatenation) <br>document.write( <br>```"<br> Convert Numbers to Strings: " + String(num1) + num2.toString()``` <br>); <br><br>  // 4. String * String: NaN  <br>(Not a Number: not a valid numeric operation) <br>```document.write("<br> String Multiplication: (NaN output)" + name * address);``` <br><br>  // 5. When the 'nothing' variable is declared  <br>but not assigned a value <br>// 'undefined': Indicates the type is unknown,  <br>and there's no value. <br>```document.write("<br> 'nothing' Variable: " + nothing);``` <br> <br> // 6. When [Cancel] is clicked in the prompt() dialog <br>// 'null': Signifies the absence of a reference object  <br>or value when none is provided. <br> ```document.write("<br> 'input' Variable: " + input);``` |  |
| 7  | doWhile | let count = 0; // Initialize a counter variable to 0 <br><br> do { <br> count++; // Increment the counter <br><br> alert("Warning " + count); <br> // Display a warning message with the current count <br><br> answer = confirm( <br>"Do you want to continue showing the warning window?"); <br>// Ask the user if they want to continue <br><br> } while (answer); <br>// Continue looping as long as 'answer' is true (user wants to continue) <br><br>  document.write("Number of Warning Alerts Shown: " + count);  <br>// Display the total number of warning alerts shown |  |
| 8 | for | let fruits = ["apple", "grape", "peach"]; <br><br>+ for <br>let sum = 0; <br>// Initialize a variable 'sum' to store the cumulative sum <br>//When 'i' increases by 2, the for loop is written as ' <br>for(let i=1; i<=10; i+=2) <br><br>  for (i = 1; i <= 10; i++) { <br>    ```document.write("i =" + i + "<br>"); ``` <br>// Display the current value of 'i' <br>// 'sum = sum + i' or 'sum += i': <br>Add the current value of 'i' to the cumulative sum <br>    sum += i;  <br>// Using the cumulative assignment operator <br>} <br><br>  ```document.write("Final value of 'i': " + i + "<br>");```  <br>// Display the final value of 'i' <br>document.write("Final value of 'sum': " + sum);  <br>// Display the final cumulative sum+ forEach() <br>fruits.forEach(function (item, index, array) { <br> console.log(item, index, array);}); <br><br>  // First parameter: Items of the array <br>// Second parameter: Index of the array <br>// Third parameter: The array itself <br>// Parameter names can be chosen arbitrarily <br>// The order of passed parameters is predetermined. <br><br>  ```+ for in``` <br> ```for (let fruit in fruits) {document.write(fruits[fruit] + "<br>");``` <br><br>  // Print the values of each element in the array <br>// Iterate through each element in the 'fruits' array <br>// Store each element in the variable 'fruit' along with its index <br><br>  + for nested <br>for (let i = 1; i <= 3; i++) { <br>  for (let j = 1; j <= 5; j++) { <br> document.write(j + " "); <br> } <br> ```document.write("<br>")```<br> } |  |
| 9  | hoisting | Refers to using a variable before it's declared in the code. <br><br>'var' allows this in some cases, <br> but it's not recommended. <br><br>'let' results in an error. |  |
| 10 | if | let a = prompt("Enter number 1"); <br>let b = prompt("Enter number 2"); <br>let c = prompt("Enter number 3"); <br><br>  // Check which of the three numbers is the largest <br>let max = a; <br>if (b > max) max = b; <br>if (c > max) max = c; <br><br>  // Display the maximum value <br>document.write('Maximum value: ' + max); <br><br>  -------------------------------------------------------------------------------------------- <br><br> // Define a predefined ID and password for comparison <br>let id = "abcd"; <br>let password = "1234"; <br><br> // Prompt the user to input their ID and password <br>let input_id = prompt("Please enter your ID"); <br>let input_password = prompt("Please enter your password"); <br><br>  //Check if the input ID and password match the predefined values <br><br>  if (input_id === id && input_password === password) { <br>// If they match, display a success message <br> document.write("Login successful"); <br>} else { <br>// If they don't match, show an alert and display a failure message <br> alert("ID or password does not match."); <br> document.write("Login failed"); <br>} <br><br>  -------------------------------------------------------------------------------------------- <br><br> let id = "abcd"; <br>let password = "1234"; <br><br> let input_id = prompt("Please enter your username"); <br>let input_password = prompt("Please enter your password"); <br><br> if(input_id == id && input_password == password){ <br> alert("Welcome, " + id + "!"); <br><br> let answer = prompt("What is the capital of South Korea?"); <br> if (answer == "Seoul") { <br>        document.write("Correct answer."); <br>} else { <br>        document.write("Incorrect. The capital of South Korea is Seoul."); <br> } <br><br> } else { <br> alert("Username or password does not match."); <br> document.write("Exiting."); <br>} | |
| 11  | let-var | let sum = 0; <br><br> // Using 'var' to declare 'i' within the 'for' loop <br>for (var i = 0; i < 10; i++) { <br> sum = sum + i; <br>} <br><br>  console.log(i); // 'i' is accessible outside the 'for' block <br><br>  sum = 0; <br>// Using 'let' to declare 'j' within the 'for' loop <br>for (let j = 1; j <= 10; j++) { <br> sum = sum + j; <br>} <br><br>  console.log(j); <br>// Error: 'j' is not defined outside the 'for' block <br>// Uncaught ReferenceError: j is not defined <br><br>  --------------------------------------------------------------------------------------------- <br><br>  // Using 'var' - Allows redeclaration of the same variable name <br>var name = "Mr.Kim"; <br>```document.write(name + "<br>");``` <br><br>  var name = "Mr.Lee"; <br>```document.write(name + "<br>");``` <br>// Attempting to redeclare a 'let' variable with the same name resu <br> lts in an error <br>let name = "Ms.Kim"; <br>```document.write(name + "<br>");``` <br>// Uncaught SyntaxError: <br> Identifier 'name' has already been declared <br>// An error occurs because 'name' has already been declared <br><br> // Using 'let' - Does not allow redeclaration  <br> of the same variable name <br>let address = "Seoul"; <br>let address = "Jeju";// Error: Cannot redeclare 'address' <br>// An error occurs because 'address' has already been declared <br>console.log(address); <br><br>  var address = "Seoul"; // Hoisting moves 'var' declarations to the top <br>console.log(address); <br>let address = "Seoul"; // Error: Cannot redeclare 'address' <br>// An error occurs because 'address' has already been declared | |
| 12 | operators | // 1. Increment/Decrement Operators: ++, -- <br>let count = 0; <br><br>  // Can be used alone; position doesn't matter <br>// count++; <br>++count; <br>console.log(count); <br><br>  // Pay attention to position <br>let x = 10; <br><br>  // Post-increment: First assign x (10) to y, then increment x by 1 <br>// let y = x++; <br><br>  // Pre-increment: First increment x by 1,  <br>then assign the updated value to y <br>let y = ++x; <br><br>  console.log(y); <br>console.log(x); <br><br>  // 2. Logical NOT Operator: ! <br>let a = true; <br>let b = !a; <br><br>  console.log(b); <br><br>  // 3. Relational Operators (Comparison Operators) <br>let n1 = 10; <br>let n2 = 20; <br><br>  console.log(n1 > n2);  // false <br>console.log(n1 != n2); // true <br>console.log(n1 == n2); // false <br><br>  // 4. Logical Operators: &&, \|\| <br>let num = 17; <br><br>  console.log(num % 3 == 0 \|\| num % 4 == 0); <br>// true if divisible by 3 or 4 <br><br>  num = 15; <br>console.log(num > n1 && num > n2); // false <br><br>  // 5. Ternary Operator (Conditional Operator): ?: <br>let score = 95; <br>let result = (score > 60) ? 'PASS' : 'FAIL'; <br>console.log(result); <br><br>  // Nested Ternary Operator <br>score = 85; <br>let grade = (score > 90 ? 'A' : (score > 80) ? 'B' : 'C'); <br>console.log(grade); <br><br>  // 6. Compound Assignment Operators <br>let sum = 10; <br>let n = 20; <br><br>  // Equivalent to: sum = sum + n; <br>sum += n; <br>console.log(sum); // 30 | |
| 13 | prompt  | // Declare a variable  <br>and store the value entered in the prompt() function <br><br>  let answer = prompt("What is your favorite fruit?"); <br>document.write("Your favorite fruit is " + answer + "."); <br><br>  ------------------------------------------------------------------------------------------- <br><br>  /* <br>  Multiline comment <br>*/ <br>// Single-line comment <br>let pay_card = confirm("Would you like to pay with a card?"); <br><br>  if (pay_card == true) { <br>  let num_card = prompt("Please enter your card number.", "xxxx-xxxx-xxxx-xxxx"); <br>  document.write("Card number: " + num_card + "."); <br>} else { <br>  document.write("You are not paying with a card."); <br>} | |
| 14 | switch | let input = prompt("Please enter a number", "1 or 2 or 3"); <br><br>  // The value received from prompt() is treated as a string <br>switch (input) { <br> case "1": ```document.write("<img src='image/lizard.png'>");``` <br> case "2": ```document.write("<img src='image/lizardon.png'>");``` break; <br> case "3": ```document.write("<img src='image/megalizardon.png'>");``` break; <br> default: alert("You entered an incorrect value"); <br>// In case a value other than 1, 2, or 3 is entered <br>} | |
| 15 | typeConversion | /*Values received from the prompt() function are treated <br> as strings. <br> When performing operations: <br> + is a string concatenation operator, <br>  so it concatenates two strings. <br> * does not apply to string operations, <br> so it automatically performs <br> numeric operations (automatic type conversion). <br>*/ <br><br>  /* Type conversion is possible only when the value is <br> in a numeric format. <br> Non-numeric strings cannot be converted. <br> "Hong_gil_dong": Cannot be converted to a number <br> (+: string concatenation / *: NaN, not a number) <br> "a": Cannot be converted to a number <br> "123": Conversion is possible <br> "123.45": Conversion is possible <br>*/ <br><br>  // (1) Without type conversion: Input is treated as a string <br>let num1 = prompt("Enter number 1"); <br>let num2 = prompt("Enter number 2"); <br><br> // Arithmetic operations <br>```document.write(num1 + num2 + "<br>");``` <br>// String concatenation (+: concatenation) <br>document.write(num1 * num2);  <br>// Automatic type conversion to numeric <br><br> // (2) With type conversion: Input is converted to an integer <br>// let num1 = parseInt(prompt("Enter number 1")); <br>// let num2 = parseInt(prompt("Enter number 2")); <br><br>  // // Arithmetic operations <br>```// document.write(num1 + num2 + "<br>"); // Numeric operation``` <br>// document.write(num1 * num2); <br><br>  // (3) With type conversion: Input is converted <br> to a floating-point number <br>// let num1 = Number(prompt("Enter number 1")); <br>// let num2 = Number(prompt("Enter number 2")); <br><br>  // // Arithmetic operations <br>// document.write(num1 + num2 + "<br>");  <br>// Numeric operation <br>// document.write(num1 * num2);  <br>// Recognized as numbers (automatic type conversion) |  |
| 16  | variable | variables <br><br> // Explicit declaration <br>let num = 10, pi = 3.14; <br>let ch = 'a'; <br>let name = "Hong_gil_dong"; <br><br>  // Implicit declaration with direct value assignment <br>address = "Seoul 1st street"; <br>nation = 'South Korea';  <br>// Both single and double quotes can be used for strings <br><br>  // Output variable values <br>console.log(num); <br>console.log(pi); <br>console.log(ch); <br>console.log(name); <br>console.log(address); <br>console.log(nation); <br><br>  // Concatenate variables with strings and display <br>```document.write("Name: " + name + "<br>");``` <br> document.write("Address: " + address);--------------------------------------------------------------------------------------------- <br>variable_global <br><br> // Variables declared in this area are global variables <br> whether you use let/var or not. <br>let x = 5, y = 10; // Explicit declaration with let: global variable <br>total = 0; // Declaration without let: global variable <br><br> // Function 1 scope <br>function f1() { <br> // Can use global variables <br> x = x + y; <br> ```document.write(x + "<br>");``` <br>} <br><br> // Function 2 scope <br>function f2() { <br> // Can use global variables <br> total = x + y; <br> document.write(total + "<br>"); <br>} <br><br> // Variables declared here are also global <br> because they are not inside a function. <br>let name = "Kim"; <br><br> // Function calls (using functions) <br>// Functions won't do any work unless they are called. <br>// To execute a function, it must be called. <br>// Calling: Simply write the function name. <br>f1(); <br>f2(); <br><br> // Global variables can be accessed here <br>```document.write("x: " + x + "<br>");``` <br>  ```document.write(name + "<br>");``` <br><br>  function showName() { <br> // Can use global variables <br> document.write("Name: " + name); <br>} <br><br> showName(); <br><br>  -------------------------------------------------------------------------------------------- <br><br> variable_local <br><br>  //Local variables:Variables declared with let/var inside a function <br>function f1() { <br> let localVar1 = 100; <br>//Variable declared with let inside the function: Local variable <br>//Local variable: Can only be used within the function f1() <br> name = "Hong_gil_dong"; <br>//Variable declared without let inside the function: Global variable <br>} <br><br> function f2() { <br> // Cannot access the local variable localVar1 declared <br> in f1(): Error <br> // document.write(localVar1); <br> // Uncaught ReferenceError: localVar1 is not defined <br> // Global variable name can be accessed <br> document.write(name); <br>} <br><br>  // Function calls <br>f1(); <br>f2(); |  |
| 17  | while  | // Initialization is mandatory <br>let i = 1; <br><br>  while (i <= 5) { // Condition <br> ```document.write(i + "<br>")```; <br> // Increment or decrement must be set <br> i++; <br>} <br> document.write("End"); <br><br>  -------------------------------------------------------------------------------------------- <br><br> let num = parseInt(prompt("Enter a number")); <br> let count  = 1; <br>         while (count <= num) { <br> if (count % 3 == 1) { <br> ```document.write("<img src='image/cherry.png'>")```<br> } else if (count % 3 == 2) { <br> ```document.write("<img src='image/bomb.png'>")``` <br> } else { <br> ```document.write("<img src='image/apple.png'>")``` <br> } <br> count++; <br> } <br><br>  -------------------------------------------------------------------------------------------- <br><br> let num = parseInt(prompt("Enter a number")); <br> let count = 1; <br>         while (count <= num) { <br> switch (count % 3) { <br> case 1: <br> ```document.write("<img src='image/cherry.png'>");``` <br>  break; <br> case 2: <br> ```document.write("<img src='image/bomb.png'>");``` <br> break; <br> case 0: <br> ```document.write("<img src='image/apple.png'>");``` <br> break; <br> } <br> count++; <br> } |  |
| 18  | write | // The head part is displayed first <br><br> // Using document.write() to display content <br>document.write("hello"); <br>document.write("<h3>Title</h3>");  <br>// HTML tag effect applied <br><br>  // Using console.log() to display content <br>console.log("hello2"); <br>console.log("<h3>Title2</h3>")  <br>// HTML tag effect not applied, <h3> is treated as a string <br>// "<h3>Title2</h3>" is printed as-is |  |
| ```</script>``` | | | |


<br>
<br>

***

<br>
<br>


# Javascript Part 2





| ```<script>``` 	|  	|  	|  	|
|:---:	|:---:	|:---	|:---	|
| 1 	| Document <br>Object <br>Model (DOM) | createElement | ```window.onload = function()``` {<br> // Step 1: Create elements (nodes)<br> // ```Create an <h3> element and store it in a variable``` <br> (object reference variable)<br> ```let h3 = document.createElement('h3');``` <br>// Create a text node to hold the text content<br> ```let text = document.createTextNode("Text to display: Hong-gil-dong");```<br><br> // Step 2: Add the created elements to the document<br> // ```Connect the text to the <h3> element using appendChild()``` <br> ```h3.appendChild(text);``` <br><br> // ```Attach the <h3> element to the body of the document``` <br> ```document.body.appendChild(h3);```<br>}; <br><br> *** <br><br> ```window.onload = function()``` {<br> // Step 1: Create elements (nodes) and set attributes<br> ```let img = document.createElement('img');``` <br> // Set attributes<br> ```img.src = "image/bird.jpg";``` <br> ```img.width = 250;``` <br>    ```img.height = 180;```<br> ```img.title = "Beautiful Bird";```<br><br> ```let img2 = document.createElement('img');``` <br> // Set attributes<br> ```img2.src = "image/A.png";``` <br> ```img2.width = 250;``` <br> ```img2.height = 180;``` <br> ```img2.title = "Beautiful Bird";``` <br><br> // Step 2: Display in the document: <br> ```Append to the body using appendChild()``` <br> ```document.body.appendChild(img);``` <br> ```// document.body.appendChild("Text");``` <br> // Uncomment to add text<br> ```// document.body.appendChild(img2);``` <br> // Uncomment to add another image<br> ```// document.body.append(img, "Text", img2);``` <br> // Uncomment to add multiple elements<br>}; |
| 2 	|  	| ```getElement(s)By``` 	| ```Id```<br><br>```<head>```<br>    ```<script>```<br> ```function change()``` {<br> // Find the element with the id 'header' and get its reference<br> ```let header = document.getElementById('header');``` <br><br> // Change the text content of the header element <br> using the innerHTML property <br> ```header.innerHTML = "Changed Title";``` <br> }<br> ```</script>``` <br> ```</head>``` <br> ```<body>``` <br> ```<h2 id="header">Title</h2>``` <br> ```<button id="btn" onClick="change()">Change Title</button>``` <br> ```</body>``` <br><br> *** <br><br>```Id (2)```<br><br>```<head>```<br> ```<meta charset="UTF-8">``` <br> ```<meta name="viewport" content="width=device-width, initial-scale=1.0">``` <br> ```<title>Change Image</title>``` <br> ```<script>``` <br> ```function change()``` {<br> ```let imgA = document.getElementById('imgA');```<br> ```let hint = document.getElementById('hint');``` <br> ```let changeButton = document.getElementById('changeButton');``` <br><br> // Setting attributes (updated values)<br> ```imgA.src = "image/B.png";``` <br> ```hint.innerHTML = "The image has been changed to a new one.";``` <br> ```hint.style.background = "red";``` <br> ```changeButton.innerHTML = "Change Complete";``` <br>}<br> ```</script>``` <br> ```</head>``` <br> ```<body>``` <br> ```<img id="imgA" src="image/A.png" alt="">``` <br>    ```<div id="hint">Press the [Change] button to switch the image.</div>``` <br> <br><br><br> ```<button id="changeButton" onclick="change()">Change</button>``` <br> ```</body>```<br> ```</html>``` <br><br> *** <br><br> ```TagName``` <br> ```<style>``` <br> ```#box { margin: 0 auto; width: 70%; text-align: center;}``` <br> ```td {width:50px; height: 30px;}``` <br> ```table``` {<br> ```margin: 0 auto; width: 70%; text-align: center;``` <br> }<br> ```</style>``` <br> ```<script>``` <br> ```let tdArr = document.getElementsByTagName('td');``` <br><br> ```function setNumberBtn()```{<br> ```for(let i=0; i<tdArr.length; i++) ``` { <br>```tdArr[i].innerHTML = i+1;``` <br> }<br> }<br><br> ```function setColorBtn()``` {<br> ```let colorB = new Array(7);``` <br> ```colorB = ['red','orange','yellow','green','blue','navy','violet'];``` <br> <br> ```for(let j=0; j<tdArr.length; j++)```{<br> ```tdArr[j].style.backgroundColor = colorB[j];``` <br> }<br> }<br><br> ```function clearNumberBtn()```{<br> ```for(let i=0; i<tdArr.length; i++)``` {<br> ```tdArr[i].innerHTML = "";``` <br> }<br> }<br><br> ```function clearColorBtn()```{ <br> ```for(let j=0; j<tdArr.length; j++)```{<br> ```tdArr[j].style.backgroundColor = 'white';``` <br> }<br>}<br><br><br>    ```</script>```<br>```</head>```<br>```<body>```<br> ```<div id="box">```<br> ```<table border="1">```<br>```<tr>```<br> ```<td></td><td></td><td></td><td></td>``` <br> ```<td></td><td></td><td></td>``` <br> ```</tr>``` <br> ```</table>``` <br><br><br> ```<button id="setNumberBtn" onclick="setNumberBtn()">``` <br> Fill Cells with Numbers<br> ```</button>``` <br> ```<button id="setColorBtn" onclick="setColorBtn()">``` <br> Fill Cells with Colors<br> ```</button>``` <br><br><br><br> ```<button id="clearNumberBtn" onclick="clearNumberBtn()">``` <br> Clear Numbers in Cells<br> ```</button>```<br> ```<button id="clearColorBtn" onclick="clearColorBtn()">``` <br> Clear Colors in Cells<br>```</button>```<br><br><br><br>```</div>```<br>```</body>```<br><br> *** <br><br>```<head>```<br> ```<meta charset="UTF-8">```<br> ```<meta name="viewport" content="width=device-width, initial-scale=1.0">```<br> ```<title>Cell Manipulation</title>``` <br> ```<style>``` <br> ```#box { margin: 0 auto; width: 70%; text-align: center;}``` <br> ```td {width:50px; height: 30px;}``` <br> ```table```{ <br> ```margin: 0 auto; width: 70%; text-align: center;``` <br>}<br> ```</style>```<br>```</head>```<br>```<body>```<br>    ```<div id="box">```<br> ```<table border="1">```<br> ```<tr>```<br> ```<td></td><td></td><td></td><td></td>``` <br> ```<td></td><td></td><td></td>``` <br> ```</tr>```<br> ```</table>``` <br><br><br> ```<button id="setNumberBtn">Fill Cells with Numbers</button>``` <br> ```<button id="setColorBtn" >Fill Cells with Colors</button>``` <br><br><br><br> ```<button id="clearNumberBtn" >Clear Numbers in Cells</button>``` <br> ```<button id="clearColorBtn">Clear Colors in Cells</button>``` <br><br><br> ```</div>``` <br><br> ```<script><br> window.onload = function()``` {<br> ```let tdArr = document.getElementsByTagName('td');```<br><br> ```let setNumberBtn = document.getElementById('setNumberBtn');```<br> ```let setColorBtn = document.getElementById('setColorBtn');``` <br> ```let clearNumberBtn = document.getElementById('clearNumberBtn');``` <br> ```let clearColorBtn = document.getElementById('clearColorBtn');```<br><br> ```setNumberBtn.onclick = function()``` {<br> ```for(let i=0; i<tdArr.length; i++)``` {<br> ```tdArr[i].innerHTML = i+1;```<br> }<br> }<br><br> ```setColorBtn.onclick = function()``` {<br> ```let colorB = new Array(7);``` <br> ```colorB = ['red','orange','yellow','green','blue','navy','violet'];``` <br><br> ```for(let j=0; j<tdArr.length; j++)```{<br> ```tdArr[j].style.backgroundColor = colorB[j];``` <br>}<br>}<br><br>            ```clearNumberBtn.onclick = function()``` {<br> ```for(let i=0; i<tdArr.length; i++)``` {<br> ```tdArr[i].innerHTML = "";```<br> }<br>}<br><br>```clearColorBtn.onclick = function()``` {<br> ```for(let j=0; j<tdArr.length; j++)```{<br> ```tdArr[j].style.backgroundColor = 'white';```<br> }<br> }<br> }<br> ```</script>``` <br> ```</body>``` <br> ```</html>``` 	|
| 3 	|  	| location 	| ```<head>```<br> ```<meta charset="UTF-8">``` <br> ```<meta name="viewport" content="width=device-width, initial-scale=1.0">``` <br> ```<title>DOM Manipulation</title>``` <br> ```<script>```<br> ```// Finding DOM elements by ID: getElementById()``` <br><br> //(1) Incorrect location <br> // Before the body element is created <br> ```// let box = document.getElementById('box');``` <br> ```// box.innerHTML = "Changed";``` <br> <br> // (3) Correct location <br> // (3) JavaScript executes after the window has loaded<br>``` window.onload = function()``` {<br> ```let box = document.getElementById('box');```<br> ```box.innerHTML = "Changed";``` <br>};<br><br> // Arrow function can also be used<br> ```// window.onload = () =>``` {<br> ```//let box = document.getElementById('box');``` <br> ```// box.innerHTML = "Changed";``` <br> // };<br><br>    ```</script>```<br>```</head>```<br>```<body>```<br> ```<div id="box">box</div>```<br> ```<script>```<br> ```// Finding DOM elements by ID: getElementById()``` <br><br> ```//(2) After HTML element creation, using DOM object``` <br> ```let box = document.getElementById('box');``` <br> ```box.innerHTML = "Changed";``` <br> ```</script>```<br> ```</body>``` <br>```</html>``` 	|
| 4 	|  	| querySelector(All) 	|   	|
| 5 	| event 	| addEventListener 	| ```<head>```<br> ```<meta charset="UTF-8">``` <br> ```<meta name="viewport" content="width=device-width, initial-scale=1.0">```<br> ```<title>DOM - addEventListener()</title>``` <br> ```<script>``` <br><br> ```window.onload = function()``` {<br> ```let btn = document.getElementById('btn');``` <br><br> ```// Applying two regular functions to the btn object``` <br> ```btn.addEventListener("click", show1);``` <br> ```btn.addEventListener("click", show2);``` <br> };<br><br> ```function show1()``` {<br>```alert("Execution 1")```<br> }<br><br> ```function show2 ()``` {<br> ```alert("Execution 2");```<br> }<br><br> ```</script>```<br> ```</head>``` <br> ```<body>``` <br> ```<button id="btn">Click</button>``` <br> ```</body>``` <br> ```</html>``` <br><br> *** <br><br>```<head>```<br> ```<meta charset="UTF-8">``` <br> ```<meta name="viewport" content="width=device-width, initial-scale=1.0">``` <br> ```<title>DOM - addEventListener()</title>``` <br> ```<script>```<br> ```window.onload = function()``` {<br> ```let h2 = document.getElementById('h2');```<br> ```let greenBtn = document.getElementById('greenBtn');``` <br> ```let redBtn = document.getElementById('redBtn');```<br> ```let yellowBtn = document.getElementById('yellowBtn');``` <br> ```let blueBtn = document.getElementById('blueBtn');``` <br><br> ```greenBtn.addEventListener("click", function()```{<br> ```h2.style.color = 'green';``` <br> });<br><br> ```redBtn.addEventListener("click", function()``` {<br> ```h2.style.color = 'red';``` <br>});<br><br> // Using an arrow function<br> ```yellowBtn.addEventListener("click", () => {``` <br> ```h2.style.color = 'yellow';``` <br>});<br><br> // Using a regular function <br>              (calling the function name without parentheses)<br> ```blueBtn.addEventListener("click", changeToBlue);``` <br> };<br><br> ```function changeToBlue()``` {<br> ```let h2 = document.getElementById('h2');```<br> ```h2.style.color = 'blue';``` <br> }<br> ```</script>``` <br> ```</head>``` <br> ```<body>``` <br> ```<h2 id="h2">Change Text Color</h2>``` <br> ```<button id="greenBtn">Green</button>``` <br> ```<button id="redBtn">Red</button>``` <br> ```<button id="yellowBtn">Yellow</button> ``` <br> ```<button id="blueBtn">Blue</button>``` <br> ```</body>```	|
| 6 	|  	| mouseEvent 	| ```<head>```<br> ```<meta charset="UTF-8">``` <br> ```<meta name="viewport" content="width=device-width, initial-scale=1.0">```<br> ```<title>DOM - addEventListener()</title>``` <br> ```<script>```<br> ```window.onload = function()``` {<br> ```let h2 = document.getElementById('h2');``` <br> ```let greenBtn = document.getElementById('greenBtn');``` <br> ```let redBtn = document.getElementById('redBtn');``` <br> ```let yellowBtn = document.getElementById('yellowBtn');```<br> ```let blueBtn = document.getElementById('blueBtn');``` <br><br> ```greenBtn.addEventListener("click", function()``` {<br> ```h2.style.color = 'green';```<br> });<br><br> ```redBtn.addEventListener("click", function()``` {<br> ```h2.style.color = 'red';```<br> });<br><br> // Using an arrow function <br> ```yellowBtn.addEventListener("click", () =>``` {<br> ```h2.style.color = 'yellow';``` <br> });<br><br> // Using a regular function <br> (calling the function name without parentheses)<br> ```blueBtn.addEventListener("click", changeToBlue);```<br> };<br><br> ```function changeToBlue()``` {<br> ```let h2 = document.getElementById('h2');```<br> ```h2.style.color = 'blue';```<br> }<br> ```</script>```<br> ```</head>``` <br> ```<body>``` <br> ```<h2 id="h2">Change Text Color</h2>``` <br>    ```<button id="greenBtn">Green</button>``` <br> ```<button id="redBtn">Red</button>``` <br> ```<button id="yellowBtn">Yellow</button>``` <br>    ```<button id="blueBtn">Blue</button>```<br> ```</body>``` 	|
| 7 	|  	| same_obj 	| ```function show1() {``` <br> ```alert("Execution 1")``` <br>}<br><br> ```function show2 ()``` {<br> ```alert("Execution 2");```<br>}<br><br> ```window.onload = show1;``` <br> // Assignment is done in this part<br><br> ```window.onload = show2;``` <br> // However, the assignment for show1 is overridden in this part 	|
| 8 	| function 	| function 	| Arrow Function<br><br>A concise way of using functions by using the arrow (=>) syntax instead of the function keyword.<br><br><br>// A self-invoking function that runs automatically without the need for <br>  a function call<br>```(function()``` {<br>    ```alert("Runs automatically without a function call");```<br>})();<br><br>// Function declaration (definition)<br> ```function show()``` {<br> ```alert("This is the show() function");```<br>}<br><br>```show();``` // ```Call the show() function```<br><br> *** <br> // Calling a Function from Within Another Function<br><br>```function start() {```<br> ```let answer = confirm("Do you want to change the background color?");```<br> ```if (answer) {```<br> // Call the function that changes the background color<br>```changeColor();``` <br>} else {<br> ```alert("Cancelled");```<br> }<br>}<br><br>// Function that changes the background color<br> ```function changeColor()``` {<br> ```let color = prompt("Enter a color ");``` <br> ```// You can enter values like red, green, #ff00ff, #777``` <br> ```document.write(color);``` <br> ```document.bgColor = color;``` <br>}<br><br> ```start();``` // Call the function<br><br><br> *** <br><br>```<head>```<br> ```<meta charset="UTF-8">``` <br> ```<meta name="viewport" content="width=device-width, initial-scale=1.0">``` <br>    ```<title>Functions</title>``` <br> ```<script>``` <br> ```function changeColor() {``` <br> ```let color = prompt("Enter a color ");``` <br> ```// You can enter values like red, green, #ff00ff, #777``` <br> ```document.bgColor = color;``` <br>}<br> ```</script>``` <br> ```</head>``` <br> ```<body>``` <br> ```<button onclick="changeColor()">Change Color</button>``` <br> ```</body>``` <br><br> *** <br><br> ```let num = 0;``` <br><br> ```function input(){``` <br> ```num = prompt("Enter a number");``` <br> ```show();``` <br> }<br><br> ```function show()``` {<br> ```for(let i=0; i<num; i++){``` <br> ```document.write("★")```<br>  }  <br>  }<br><br> ```input();``` |
| 9 	|  	| anonymous 	| // Anonymous function: Store function code in a variable<br> ```let sum = function(a, b)``` {<br> ```document.write(a + b + "<br>");```<br>};<br><br>// Call the anonymous function: <br>  Use the variable name as the function name<br> ```sum(10, 20);``` <br><br>// Anonymous function with a return value<br> ```let add = function(a, b) {``` <br> ```return a + b;``` <br>};<br><br> ```document.write(add(10, 20));```|
| 10 	|  	| arrow 	| // Using an arrow function as an anonymous function<br>// A function that takes two values and returns their sum<br>// Parameters 'a' and 'b' receive the two values<br>// Return statement is omitted<br>```let sum = (a, b) => a + b;```<br><br>/* <br> The arrow function above is a shortened version of the following <br>    function:<br> ```let sum = function(a, b)``` {<br> ```return a + b;``` <br> }<br>*/<br><br>```alert(sum(1, 2));``` // Outputs 3<br><br> *** <br><br> ```let nums = [1, 2, 3, 4, 5];``` <br><br>// Using forEach to iterate through an array <br>  and print each element multiplied by 10<br>```nums.forEach(x => {```<br> ```console.log(x * 10);```<br>});<br><br> ```let fruits = ["apple", "grape", "watermelon"];``` <br><br>// Using forEach to iterate through an array <br>   and print each element along with its index and the original array<br> ```fruits.forEach((item, index, fruits) => {``` <br> ```console.log(item, index, fruits);``` <br>}); 	|
| 11 	|  	| callback 	| // Callback function definition<br> ```function callback() {``` <br> ```console.log("Callback function called");``` <br>}<br><br> ```console.log("Before calling the callback function");``` <br><br> // Setting a timeout: Call the callback function after 3 seconds<br>```setTimeout(callback, 3000);```<br><br>```console.log("After calling the callback function");```<br><br> *** <br><br>// Synchronous Callback function<br>```let callback = function() {```<br> ```console.log("Callback function called");``` <br>};<br><br>// Function that receives a callback function<br> ```function show(callback) {``` <br> ```callback();``` // Calling the callback function<br>}<br><br> ```console.log("Before calling the callback function");``` <br><br>// ```Calling the show() function and passing the callback function``` <br> ```show(callback);``` <br><br> ```console.log("After calling the callback function");```<br><br> *** <br><br>// Anonymous function: a callback function to be passed <br>  ```to the show() function``` <br> ```let callback = function() {``` <br> ```alert("This is an anonymous function");``` <br>};<br><br> // Function that accepts another function as a parameter<br> ```function show(a) { // The parameter name can be anything (e.g., 'a')``` <br> // Calling the function received as a parameter within the function<br> ```a();```<br>}<br><br>// ```Calling the show() function and passing the anonymous function (callback) as an argument``` <br> ```show(callback);``` <br> ```// When calling the show() function,``` <br> ```the anonymous function assigned to the 'callback' variable is passed``` <br> ```as an argument.``` <br> // 'callback' is just a variable name.<br><br> *** <br><br> // A regular function to be used as a callback function <br> ```function end() {``` <br> ```console.log("End");``` <br> } <br> <br> // A function that takes a regular function as a parameter <br>  and uses it as a callback function <br> ```function showName(name, callback) {```<br> ```console.log('Name: ', name);``` <br> ```callback();``` <br>// Calling the callback function received as a parameter <br> ```(executes the 'end()' function)```<br>}<br><br> ```// Calling the 'showName()' function with a name and a regular``` <br> ```function passed as a callback<br>showName('Ms.Kim', end);```<br>// 'end' is the function name passed as a callback. 	|
| 12 	|  	| closure 	| ```Closure (closure):``` <br><br> A function that remembers the environment <br>(lexical environment) in which it was created.<br><br>Cases where closures are useful:<br>When you need to remember the current state <br>and maintain the most recent changes.<br><br>Example: Implementing a counting feature.<br>When a global variable is declared and initialized to 0:<br>```let count = 0;```<br>In other places (functions), it can be changed.<br>Functions that increase the count:<br><br>```function increment() {```<br>  // Increase by 1<br> ```count++;```<br>}<br><br>When you declare it as a local variable <br>with an initial value of 0 within a function:<br><br>The value is reset to 0 every time the function is called <br>(it doesn't remember the changed value).<br><br>In such cases, closures are used <br>to remember the most recent changed value.<br><br>Closures are created using anonymous functions <br>or immediately invoked functions, returning the result (function).<br> *** <br><br>// Global variable declaration and initialization<br> ```let count = 0;``` <br><br>// Function that increases the value of count<br> ```function increase() {``` <br> ```return ++count;```<br>}<br><br>```console.log(increase());```<br>```console.log(increase());```<br>```console.log(increase());```<br><br> *** <br><br>// Using a closure for counting<br> ```function increase() {``` <br> ```let count = 0;``` // Local variable within the function<br><br> // Return a closure (function) that increments and returns the count<br> ```return function() {``` <br> ```return ++count;``` <br>}<br>}<br><br>```let count = increase();``` <br> // Initialize a variable 'count' with the closure returned by 'increase'<br><br> ```console.log(count());``` <br> // Call the closure to increase and display the count<br> ```console.log(count());``` <br>// Call the closure again to further increase and display the count<br> ```console.log(count());``` <br>// Call the closure once more to increase and display the count<br><br> *** <br><br>// Using an immediately invoked function expression (IIFE) <br>to create a closure for counting<br> ```let count = (function() {``` <br> ```let counter = 0;``` <br><br>// Return a closure (function) that increments and returns the counter<br> ```return function() {``` <br> ```return ++counter;``` <br> }<br>})();<br><br> ```console.log(count());``` <br>// Call the closure to increase and display the count<br><br> ```console.log(count());``` <br>// Call the closure again to further increase and display the count <br><br> ```console.log(count());``` <br> // Call the closure once more to increase and display the count 	|
| 13 	|  	| default_param 	| // Function with default parameters<br>// If values are not provided, default parameters are used.<br>// If values are provided, they override the defaults. <br> ```function rect(width = 10, height = 20) {``` <br> ```document.write("width : " + width + "<br>");``` <br> ```document.write("height : " + height  + "<br>");``` <br> ```document.write("------------------<br>");```<br> ```return width * height;``` <br>}<br><br> ```console.log(rect());``` <br>// Using default parameters<br>```console.log(rect(30));```<br>// 1 parameter provided, first parameter is used,<br> second one uses default<br> ```console.log(rect(100, 200));``` <br>// 2 parameters provided, default parameters are not used<br><br>```document.write("<hr>");```<br><br>// Using regular parameters along with default parameters<br>// Default parameters are placed at the end<br>```function showInfo(name, score, year = 4) {```<br> ```document.write("name : " + name + "<br>");``` <br> ```document.write("score : " + score + "<br>");``` <br> ```document.write("year : " + year + "<br>");``` <br>}<br><br> ```showInfo();``` <br> // undefined, undefined, 4<br> ```showInfo("John");``` <br>// John, undefined, 4<br> ```showInfo("John", 80);``` <br>// John, 80, 4<br> ```showInfo("John", 80, 1);``` <br>// John, 80, 1 	|
| 14 	|  	| hoisting 	| // Function Hoisting: Calling a function before it is declared (defined). <br> ```show();``` // Function hoisting is possible.<br><br>```function show() {```<br> ```alert("This is the show() function.");``` <br>} 	|
| 15 	|  	| param 	| // Accepting five numbers as input and storing them in an array<br> ```function input() {``` <br> ```let arr = new Array(5);``` <br><br> ```for (let i = 0; i < arr.length; i++) {```<br> ```arr[i] = parseInt(prompt("Enter the " + (i + 1) + "th number"));``` <br> }<br><br> ```document.write("The maximum value is " + max(arr));```<br>}<br><br>// Taking an array as input and returning the maximum value<br> ```function max(num) {```<br> ```let maximum = num[0];``` <br> ```for (let i = 1; i < num.length; i++) {``` <br> ```if (num[i] > maximum)``` <br> ```maximum = num[i];``` <br> }<br> ```return maximum;``` <br>}<br><br> ```input();```<br><br> *** <br><br>// A function that takes deposit amount and interest rate as input<br>// and calculates and prints the deposit interest<br> ```function input() {``` <br> ```let deposit = parseInt(prompt("Enter the deposit amount"));``` <br> ```let rate = Number(prompt("Enter the interest rate"));``` <br><br>  // Call the calInterest() function to calculate the deposit interest<br>  // Pass the deposit amount and interest rate as arguments<br> ```calInterest(deposit, rate);```<br>}<br><br>// A function that takes deposit amount and interest rate as parameters<br>// and calculates and prints the deposit interest<br>// Parameters: deposit, rate<br>// Use the parameters as local variables in the function<br> ```function calInterest(deposit, rate) {``` <br> // Calculate the deposit interest<br> ```let interest = deposit * rate / 100;``` <br> ```document.write("Deposit interest: " + interest + " won");``` <br>}<br><br>// Call the input() function to start the process<br> ```input();``` <br><br> ```<body onload="input()">``` <br> <br> ```</body>``` <br><br> *** <br><br>// Accepting the number of rows and columns as input<br>// and calling the makeTable() function with these values<br> ```function input() {``` <br> ```let row = parseInt(prompt("Enter the number of rows"));``` <br> ```let col = parseInt(prompt("Enter the number of columns"));``` <br> ```makeTable(row, col);``` // Calling the function with arguments <br>}<br><br>// Creating a table based on the provided number of rows and columns<br> ```function makeTable(row, col) {``` <br> ```document.write("<table border='1'>");``` <br> ```for (let i = 0; i < row; i++) {``` <br> ```document.write("<tr>");``` <br> ```for (let j = 0; j < col; j++) {``` <br> ```document.write("<td>★</td>");```<br> }<br> ```document.write("</tr>");``` <br> }<br> ```document.write("</table>");``` <br>}<br><br> // Call the input() function to initiate the process<br> ```input();``` |
| 16 	|  	| return 	| // A function that takes two numbers as input, adds them together, <br>and returns the result<br> ```function add() {``` <br> ```let num1 = Number(prompt("Enter the first number"));``` <br> ```let num2 = Number(prompt("Enter the second number"));``` <br> // let sum = num1 + num2;<br> // return sum;<br> ```return num1 + num2;``` // Return the sum of the two numbers<br> // The return goes back to the place where the function was called<br>}<br><br>// Call the add() function, receive the result, and display it<br>```document.write("Sum: " + add());``` <br>// Call the function, receive the result, and display it<br>// The return from the add() function is at this location<br><br> *** <br><br> ```function start() {``` <br> ```let call = confirm("Do you want to input grades?");```<br> ```if (call == true) {``` <br> ```alert("Average: " + input().toFixed(2));``` // Display the average with 2 decimal places<br> } else {<br> ```alert("Exiting.");``` <br> }<br> }<br><br> ```function input() {``` <br> ```let num1 = Number(prompt("Enter the score for Korean"));```<br> ```let num2 = Number(prompt("Enter the score for English"));```<br> ```let num3 = Number(prompt("Enter the score for Math"));``` <br><br> ```return (num1 + num2 + num3) / 3;``` <br> }<br> ```</script>``` <br> ```</head>``` <br> ```<body>``` <br> ```<button onclick="start()">Input Grades</button>``` <br> ```</body>``` |
| 17 	| msgWindow 	|   	| ```<script>```<br>// Function to close the current window<br>   and give focus to the parent window<br>        ```function msgWinClose() {``` <br> ```window.close();``` <br>// Close the current window<br> ```opener.focus();``` <br>// Give focus to the parent window (window_setTimeout.html)<br> // opener: Object referring to the parent window<br> }<br> ```</script>``` <br>```</head>```<br> After loading, call the msgWinClose() function after a delay <br>of 3 seconds <br> ```<body onLoad="setTimeout('msgWinClose()', 3000)">``` <br> Notice <br><br> This window will automatically close after 3 seconds.<br> ```</body>``` |
| 18 	| object 	| array 	| ```let fruits = new Array("apple", "peach", "grape");``` <br><br> ```document.write("Displaying all elements: ");```<br> ```for (let i = 0; i < fruits.length; i++) {```<br> ```document.write(fruits[i] + " ");``` <br>}<br><br> ```fruits.shift();``` // Remove the first element<br><br> ```document.write("<br>shift(): Remove the first element: ");``` <br> ```for (let i = 0; i < fruits.length; i++) {``` <br> ```document.write(fruits[i] + " ");``` <br>}<br><br> ```fruits.unshift("pear");``` // Add an element to the front<br><br> ```document.write("``` <br> ```unshift(): Add an element (pear) to the front: ");``` <br> ```for (let i = 0; i < fruits.length; i++) {``` <br> ```document.write(fruits[i] + " "); ``` <br> }<br><br> ```fruits.pop();``` // Remove the last element<br><br> ```document.write(" ``` <br> ```pop(): Remove the last element: ");``` <br> ```for (let i = 0; i < fruits.length; i++) {``` <br> ```document.write(fruits[i] + " ");``` <br>}<br><br> ```fruits.push("strawberry");``` // Add an element to the end<br> ```fruits.push("watermelon");``` // Add an element to the end<br><br> ```document.write("``` <br> ```push(): Add elements to the end: ");``` <br> ```for (let i = 0; i < fruits.length; i++) {``` <br> ```document.write(fruits[i] + " ");``` <br>}<br><br> ```fruits.reverse();``` // Reverse the order<br> ```document.write("``` <br> ```reverse(): Reverse the order: ");``` <br> ```for (let i = 0; i < fruits.length; i++) {``` <br> ```document.write(fruits[i] + " ");``` <br>}<br><br> ```fruits.sort();``` // Sort in ascending order<br> ```document.write("```<br> ```sort(): Sort in ascending order: ");``` <br> ```for (let i = 0; i < fruits.length; i++) {``` <br> ```document.write(fruits[i] + " ");``` <br>}<br><br> ```fruits.splice(2, 1, "mango", "orange");``` <br> ```// Remove 1 element at [2] and add 'mango' and 'orange'``` <br> ```document.write("```<br>```splice(): Remove 1 element at [2] and add 'mango' and 'orange': ");```<br> ```for (let i = 0; i < fruits.length; i++) {```<br> ```document.write(fruits[i] + " ");``` <br>}<br><br>// Create a new array<br> ```let newFruits = fruits.slice(1, 4);``` <br> ```// Extract elements from [1] to [3] and store in a new array``` <br> ```document.write("``` <br> ```slice(1, 4): Extract elements from [1] to [3] and store in a new array: ");```<br> ```for (let i = 0; i < newFruits.length; i++) {``` <br> ```document.write(newFruits[i] + " ");``` <br>} 	|
| 19 	|  	| class 	| ```class Rectangle {```<br> ```constructor(width, height) {```<br> ```this.width = width;``` <br> ```this.height = height;```<br> }<br><br> ```get width() {```<br> ```return this._width;```<br> }<br><br> ```get height() {``` <br> ```return this._height;``` <br> }<br><br> ```set width(width) {``` <br> ```this._width = width;``` <br> }<br><br> ```set height(height) {```<br> ```this._height = height;```<br> }<br><br> ```getArea() {``` <br> ```return 'The area of the rectangle is ' + this.width * this.height;``` <br> }<br>}<br><br> ```let r1 = new Rectangle(30, 10)``` <br> ```console.log('Width: ' + r1.width + ' Height: ' + r1.height);``` <br> ```console.log(r1.getArea());``` <br><br> ```r1.height = 30;``` <br> ```r1.width = 20;``` <br><br> ```console.log('Width: ' + r1.width + ' Height: ' + r1.height);``` <br> ```console.log(r1.getArea());```<br><br><br> *** <br><br><br> ```class Person {```<br> ```// Constructor``` <br> ```constructor(name, age) {``` <br> ```// Define properties``` <br> ```this.name = name;``` <br> ```this.age = age;``` <br> }<br><br> ```// Getter: Returns the 'name' property (with an underscore)``` <br> ```// Method to retrieve property values``` <br> ```get name() {``` <br> ```return this._name;``` <br>}<br><br> ```get age() {``` <br> ```return this._age;``` <br> }<br><br> // Setter: Sets property values (with an underscore)<br> // The values to set are received as parameters<br> ```set name(name) {```<br> ```// Property = value (parameter);``` <br> ```this._name = name;``` <br> }<br><br> ```set age(age) {```<br> ```this._age = age;``` <br> }<br><br> // Additional methods as needed<br> // You can use properties without underscores here<br> ```toString() {``` <br> ```return this.name + " is " + this.age + ' years old.';``` <br> }<br>}<br><br>```// Object creation: new ClassName();```<br>// When an object is created, the constructor is automatically called, <br>initializing property values<br>// Instance p1<br>// Instance p2<br>// Collectively referred to as objects<br> ```let p1 = new Person('Hong Gil-dong', 25);``` <br> ```let p2 = new Person('Im Mung-ryong', 30);``` <br><br>// To retrieve property values, call the getter: ```no parentheses ()```<br> ```console.log('Name: ' + p1.name + ', Age: ' + p1.age);``` <br> ```console.log('Name: ' + p2.name + ', Age: ' + p2.age);```<br><br> // Call the toString() method <br> ```console.log(p1.toString());``` <br> ```console.log(p2.toString());``` <br><br> // Changing values: Set new values, <br>which triggers the setter: ```no parentheses ()``` <br> ```p1.age = 27;``` <br><br> ```console.log(p1.toString());``` 	|
| 20 	|  	| date 	| ```let today = new Date();``` // Declare a Date object named 'today'<br><br>// ```getYear(): 119 (getYear() is deprecated; use getFullYear() instead)``` <br> ```let year = today.getFullYear();```<br> ```let month = today.getMonth() + 1;``` <br>// Returns 0-based month (0: January); add 1 for display<br>```let date = today.getDate();```<br>```let day;``` // Variable to store the day of the week<br><br>// Set the day of the week<br>```switch (today.getDay()) {```<br> ```case 0: day = "Sunday"; break;``` <br> ```case 1: day = "Monday"; break;``` <br> ```case 2: day = "Tuesday"; break;```<br> ```case 3: day = "Wednesday"; break;``` <br> ```case 4: day = "Thursday"; break;``` <br> ```case 5: day = "Friday"; break;``` <br> ```default: day = "Saturday";``` <br>}<br><br> ```document.write("Today is " + day + ", " + month + "/" + date + "/" + year);``` <br><br> // Display the current time: ~ hours ~ minutes ~ seconds<br> ```let hour = today.getHours();``` <br> ```let minute = today.getMinutes();``` <br> ```let second = today.getSeconds();``` <br><br> ```document.write``` <br>("<br> ```It is currently " + hour + " hours " + minute + " minutes " + second + " seconds.");``` 	|
| 21 	|  	| function 	| ```function People() {``` <br> ```// Properties (attributes): Member variables``` <br> ```this.name = "Hong Gil-dong";```<br> ```this.age = 22;``` <br><br> // Method<br> ```this.getName = function () {``` <br> ```return this.name;``` <br> }<br>}<br><br>// Object (instance) creation: using 'new'<br>```let person = new People();```<br>```console.log(person.name);```<br>```console.log(person.age);```<br>```console.log(person.getName());```<br><br>// You can add properties (attributes) <br>to existing objects without changing the constructor function<br>```person.address = "Seoul";```<br><br>// You can also add methods to existing objects<br>```person.showAddress = function () {```<br> ```return this.address;``` <br>}<br><br>// Calling the newly added method<br> ```console.log(person.showAddress());``` <br><br>// (2) Passing values as parameters <br>// Using parameters received by the constructor function<br> ```function Book(title, author, price, date) {``` <br>  // ```Properties = Parameters;```<br> ```this.title = title;``` <br> ```this.author = author;``` <br> ```this.price = price;``` <br> ```this.date = date;``` <br><br> // Method<br> ```this.getBook = function () {``` <br> ```return this.title + " by " + this.author + ", Price: $" + this.price + ", Published on: " + this.date;```<br> }<br>}<br><br>// Creating an object with initial values passed <br>to the constructor function<br> ```let book = new Book("JavaScript", "Hong Gil-dong", 20000, "2023-08-01");```<br> ```console.log(book.getBook());``` 	|
| 22 	|  	| literal 	| *** <br> literal arrow <br> *** <br><br> ```// Object Creation 1: Properties + Member Method``` <br> ```let person = {``` <br> // Members<br> // (1) Properties<br> ```name: "Hong Gil-dong",``` <br> ```age: 20,```<br><br>  // (2) Method: Using arrow function <br> // !!! Arrow functions do not have their own 'this' context.<br> ```getName: () => {``` <br> ```name = "Im Mung-ryong";``` <br> ```return this.name;``` <br> // Unable to find the property value 'Hong Gil-dong' here<br> }<br>};<br> ```console.log(person.getName());```<br><br><br> *** <br><br> ```// Object Creation 1: Properties + Member Method<br>let person = {``` <br> // Members<br> // (1) Properties<br> ```name: "Hong Gil-dong",``` <br> ```age: 20,``` <br><br> // (2) Method *** Using 'this'<br> ```getName: function () {``` <br> ```return this.name;``` <br> // 'this' refers to the object itself,<br> // Use 'this' to access the object's members (properties).<br> }<br><br> // Note: When using 'this', you can access 'name' without issues.<br>};<br><br>// Using the object<br>// ```object.property, object.method()```<br>```console.log(person.name);```<br>```console.log(person.age);```<br>```console.log(person.getName());```<br><br><br>// Object Creation 2: Properties Only<br> ```let car = {```<br> ```no: '11ga1234',```<br> ```name: 'Sonata',```<br> ```maker: 'Hyundai',``` <br> ```year: '2022',```<br> ```cc: 2000,``` <br>};<br><br>// Output the value of one property<br> ```console.log(car.no);``` <br><br>// Output all property names (keys)<br> ```for (let i in car) {``` <br> ```console.log(i);``` <br>};<br><br><br>// Output all property values<br> ```for (let i in car) {``` <br> ```console.log(car[i]);``` <br>};<br><br>// Object Creation 3<br> ```let circle = {``` <br> ```center: { x: 1.0, y: 2.0 },``` <br> ```radius: 2.5,```<br><br> ```getArea: function () {```<br> ```return this.radius * this.radius * 3.14;``` <br> },<br><br> ```showCenter: function () {``` <br> ```console.log("Center: ", this.center.x, ",", this.center.y);``` <br> }<br>}<br><br>// Using the object: ```object.property, object.method``` <br> ```console.log(circle.radius);``` ```// 2.5```<br> ```console.log(circle.center.x, circle.center.y);``` ```// 1 2```<br>```console.log(circle.getArea());``` ```// 19.625```<br>// Calling a method with no return value<br> ```circle.showCenter();``` <br>```// Center: 1, 2```<br>```console.log(circle.showCenter());``` <br> // Center: 1, 2 / undefined: no return value 	|
| 23 	|  	| Math_random 	| // Generating random numbers using ```Math.random()``` <br> ```for (let i = 1; i <= 10; i++) {``` <br> // (1) Generates a random floating-point number <br> ```between 0.0x and 0.9x``` <br> ```// let num = Math.random();``` <br> ```// document.write(num + "<br>");``` <br><br> // (2) Multiplies by 10 to get integers in the range 0 to 9<br> ```// 0.1234566, 3.49494949``` <br> ```// let num = Math.random() * 10;``` <br> ```// document.write(num + "<br>");```<br><br> // (3) Rounds down to convert to integers, <br>  discarding the decimal part<br> // Converts to integers between 0 and 9<br> ```// let num = Math.floor(Math.random() * 10);```<br> ```// document.write(num + "<br>");```<br><br> // (4) Adds 1 to set the range from 1 to 10<br> // Starting from 1, generates 10 numbers<br> ```let num = 1 + Math.floor(Math.random() * 10);``` <br> ```document.write(num + "<br>");```<br>}<br><br> *** <br><br> ```let num = 1 + Math.floor(Math.random() * 3);```<br><br>// Uncomment one of the following lines to display an image <br>and its filename:<br><br>// Display an image with its filename using string concatenation<br> ```// document.write<br>("<img src='image/그림" + num + ".jpg'><br>pic" + num + ".jpg");```<br><br>// Display an image with its filename using template literals<br> ```document.write(`<img src='image/pic${num}.jpg'>```<br>```pic${num}.jpg`);``` 	|
| 24 	|  	| newObject 	| ```let person = new Object();```<br><br>// Add properties<br>```person.name = "Hong Gil-dong";```<br>```person.age = 20;```<br><br>// Add a member method<br>```person.getName = function () {```<br> ```return this.name;``` <br>}<br><br>// Using the object: ```object.property / object.method()``` <br> ```console.log(person.age);```<br> ```console.log(person.getName());``` 	|
| 25 	| string 	| charAt 	| ```let num = prompt("Please enter your resident registration number"``` <br>, ```"xxxxxx-xxxxxxx");``` <br>```// [7]```<br>```// Example input: 011011-1012345```<br><br> ```if (num.charAt(7) === '1' \|\| num.charAt(7) === '3') {``` <br> ```alert("Male");``` <br> ```} else {``` <br> ```alert("Female");```<br>}<br><br><br> *** <br><br> ```let input = prompt("Enter a number");``` <br>// The entered value is a string<br><br>// Check if the input consists of numeric characters only<br>// Numbers can be recognized as such <br>```if we don't perform addition ('+' operator)```<br><br>// This is assuming that multiplication <br>```('*' operator) has not been used previously```<br>// '0' can be used as well<br>```for (let i = 0; i < input.length; i++) {```<br> ```if (!('0' <= input.charAt(i) && input.charAt(i) <= '9')) { ``` ```// If it's not a digit,```<br> ```show an alert and exit```<br> ```alert("Not in a numeric format");``` <br> ```break;``` <br> }<br>}<br><br> ```document.write(input);``` 	|
| 26 	|  	| indexOf 	| ```let address = "1 Yeoksam-dong, Gangnam-gu, Seoul, South Korea";``` <br><br>// Returns the index if found<br> ```document.write(address.indexOf("Gangnam"));``` // 18<br><br> ```// Returns -1 if not found``` <br> ```document.write("```<br> ```", address.indexOf("Busan"));``` ```// -1```<br><br><br> *** <br><br><br> ```let area = ["Seoul", "Busan", "Incheon", "Daegu"];``` <br> ```let input = prompt("Enter one of the following areas"```<br> ```, "Seoul/Busan/Incheon/Daegu");```<br>// If the value is not found, indexOf returns -1<br> ```if (area.indexOf(input) == -1) {``` <br> ```alert("You have entered incorrectly");``` <br> ```} else {``` <br> ```alert(input + " is at index " + area.indexOf(input));``` // If found, indexOf returns the array index<br>} 	|
| 27 	|  	| interoplation 	| ```let name = "Hong Gil-dong";```<br> ```document.write(`Name: ${name}`);``` <br> ```console.log(`Name: ${name}`);``` <br><br> ```function rectangle(width = 10, height = 20) {```<br>    ```return `Area of the rectangle: ${width} x ${height} = ${width * height}`;```<br>}<br><br>```console.log(rectangle());``` ```// Default values: Width = 10, Height = 20```<br>```console.log(rectangle(30));``` ```// Width = 30, Default Height = 20```<br>```console.log(rectangle(100, 200));``` ```// Width = 100, Height = 200``` 	|
| 28 	|  	| substring 	| ```let birthday = prompt("Enter your date of birth (e.g., 19990125)");```<br><br>```let year = birthday.substring(0, 4);```<br>```let month = birthday.substring(4, 6);```<br>```let date = birthday.substring(6, 8);```<br><br>```document.write(`You were born on ${year} year ${month} month ${date} day`);``` 	|
| 29 	|  	| split 	| ```let input = prompt("Enter your date of birth (e.g., 1999-11-12)");```<br><br>```let birth = input.split("-");```<br><br>```document.write(`${birth[0]} year ${birth[1]} month ${birth[2]} day`);``` 	|
| 30 	|  	| toLocaleString 	| // Display thousands separator: ```toLocaleString()```<br>```let amount = 1000000;```<br>```document.write(amount + "<br>");```<br>```document.write(amount.toLocaleString() + "<br>");```<br><br>```// Set decimal places: toFixed(digits)```<br>```let avg = 83.33333333;```<br>```document.write(avg.toFixed(2));``` 	|
| 31 	| window 	| bigimage 	| ```<center>```<br> ```<img src="image/Gogh.jpg" title="Café Terrace at Night by Vincent van Gogh">```<br><br><br> ```<button onClick="window.close()">Close</button>```<br> ```</center>``` 	|
| 32 	|  	| location 	| ```<head>```<br> ```<script>```<br> ```function hrefMove() {``` <br> // Using the href attribute<br> ```location.href = "http://www.google.com";``` <br>}<br> ```function assignMove(){``` <br> ```window.location.assign("http://www.daum.net");``` <br>}<br> ```function replaceMove() {```<br> ```window.location.replace("http://www.youtube.com");``` <br>}<br>```</script>```<br>```</head>```<br>```<body>```<br> ```<a href="http://www.naver.com">Naver</a>``` <br><br><br><br> ```<button onClick="hrefMove()">Google</button>``` <br> ```<button onClick="assignMove()">Daum</button>``` <br> ```<button onClick="replaceMove()">YouTube</button>``` <br> ```</body>``` |
| 33 	|  	| msgWindow 	| ```<script>```<br> ```function msgWinClose() {```<br> ```window.close();``` // Close the current window<br> ```opener.focus();```<br> // Give focus to the parent window ```(window_setTimeout.html)``` <br> // opener: Object referring to the parent window<br> }<br> ```</script>``` <br> ```</head>``` <br> ```After loading, call the msgWinClose() function after 3 seconds``` <br> ```<body onLoad="setTimeout('msgWinClose()', 3000)">```<br> Announcement <br><br> This window will automatically close in 3 seconds.<br> ```</body>``` 	|
| 34 	|  	| open 	| *** <br> open new <br> *** <br><br> ```<head>``` <br> ```<script>``` <br> ```function showBigImage() {``` <br> // Center the window on the screen<br> ```let bigWin_width = 699;``` <br> ```let bigWin_height = 800;``` <br><br> ```let x = (window.screen.width / 2) - (bigWin_width / 2);``` <br> ```let y = (window.screen.height / 2) - (bigWin_height / 2);``` <br><br> ```// Open a new window with the content from "window_bigImage.html"``` <br> ```window.open("window_bigImage.html", "bigWin", "width=699, height=800, left=" + x + ", top=" + y);``` <br> }<br><br> ```function openNewWindow() {``` <br> // Create a new window and assign it to a variable<br> ```let newWindow = window.open("", "newWin", "width=400, height=150, left=300, top=20");``` <br> // Set the content for the new window using document object<br> ```newWindow.document.body.innerHTML = "Created a new window named 'newWin'";``` <br> ```newWindow.document.body.innerHTML += "<p><button onClick='window.close()'>Close</button></p>";```<br> }<br> ```</script>``` <br> ```</head>``` <br> ```<body>``` <br> ```<button onClick="showBigImage()">Open Big Image Window</button>``` <br> ```<button onClick="openNewWindow()">Open New Window</button>``` <br> ```</body>``` <br><br><br><br> *** <br><br><br> ```<head>```<br> ```<script>``` <br> ```// Function to open the "window_bigImage.html" document``` <br> ```function showBigImage() {``` <br> // Center the window on the screen <br> ```let bigWin_width = 699;```<br> ```let bigWin_height = 800;``` <br><br> ```let x = (window.screen.width / 2) - (bigWin_width / 2);``` <br> ```let y = (window.screen.height / 2) - (bigWin_height / 2);``` <br><br> ```// Open a new window displaying "window_bigImage.html"``` <br> ```window.open("window_bigImage.html", "bigWin", "width=699, height=800, left=" + x + ", top=" + y);``` <br>}<br> ```</script>``` <br> ```</head>``` <br> ```<body>``` <br> ```<center>```<br> Hover over the image to<br><br> view it larger<br><br><br><br> ```<img src="image/Gogh.jpg" width="118" height="146" onMouseOver="showBigImage()">``` <br> ```</center>``` <br> ```</body>``` 	|
| 35 	|  	| setInterval 	| ```<head>``` <br> ```<script>``` <br> // Function to display the time <br> ```function showTime() {``` <br> ```let now = new Date();``` <br><br> ```let hour = now.getHours();``` <br> ```let minute = now.getMinutes();``` <br> ```let second = now.getSeconds();``` <br><br> // Create a formatted time string <br> ```let time = `${hour} hours ${minute} minutes ${second} seconds`;``` <br> // Display the time in the element with id "timeShow" (using innerHTML) <br> ```document.getElementById("timeShow").innerHTML = time;``` <br> }<br> ```</script>``` <br> ```</head>``` <br> ```<body>``` <br> ```Set Timer button -->``` <br> ```<button onClick="timerID=setInterval('showTime()', 1000)">Display Time</button>``` <br> ```Clear Timer button ``` <br> ```<button onClick="clearInterval(timerID)">Clear Time</button>``` <br> ```<h2 id="timeShow">Time will be displayed here</h2>``` <br> ```</body>``` <br><br><br> *** <br><br> ```<head>``` <br> ```<script>``` <br> ```let count = 0;``` // Initialize the count<br><br> ```function start() {```<br> ```alert("Displaying 10 images");``` <br> ```timeID = setInterval(showImg, 1000);``` // Start the timer to display images at 1-second intervals<br> }<br><br>```function showImg() {```<br> // Display 10 images, alternating between two images<br><br> // Increase the count by 1 each time the function <br> is called at 1-second intervals<br> ```count++;``` <br><br> ```if (count % 2 == 1) {``` <br> ```document.write("<img src=image/apple.png>");``` <br>// Display an apple image<br> ```} else {``` <br> ```document.write("<img src=image/bomb.png>");``` <br> // Display a bomb image<br> }<br><br>  // If the count reaches 10, clear the timer<br> ```if (count == 10)``` <br> ```clearTimeout(timeID);``` <br> }<br> ```</script>``` <br> ```</head>``` <br> ```<body onload="start()">``` <br> ```</body>``` <br><br> *** <br><br><br> ```<head>``` <br> ```<script>``` <br> ```let count = 0;``` <br><br> ```function startCount() {``` <br> ```timeID = setInterval(showCount, 500);``` <br> }<br><br> ```function showCount() {``` <br> ```count++;``` <br> ```countShow.innerHTML = 11 - count;```<br> ```bombShow.src = "image/bomb" + count + ".jpg";``` <br> ```if (count == 11) {``` <br> ```clearTimeout(timeID);``` <br> ```count = 0;``` <br> }<br> }<br> ```</script>``` <br> ```<style>``` <br> ```body {```<br> ```width: 150px;``` <br> ```margin: 0 auto;``` <br> ```text-align: center;``` <br> }<br> ```</style>``` <br> ```</head>``` <br> ```<body>``` <br> ```<h2 id="countShow">10</h2>``` <br> ```<img id="bombShow" src="image/bomb1.jpg">``` <br> ```<button onClick="startCount()">Start Countdown</button>``` <br> ```</body>``` <br><br><br> - There are two functions, startCount() and showCount(). The startCount() function starts a countdown timer by calling the showCount() function every 500 milliseconds.<br><br>The showCount() function increments the count, updates the displayed countdown value, and changes the source of an image to display different bomb images as the count progresses. When the count reaches 11, it clears the timer using clearTimeout and resets the count.<br><br>The CSS style rule centers the content within the body of the page and sets the width to 150 pixels.<br><br>The page displays an initial countdown value of 10, an image of a bomb, and a button labeled "Start Countdown." When the button is clicked, the countdown begins, and the displayed values change accordingly. 	|
| 36 	|  	| setTimeout 	|  ```<script>``` <br>  // ```Function to open the "msgWindow.html" window``` <br> ```function openMessageWindow() {``` <br> ```window.open("msgWindow.html", "msgWin", "width=400, height=200");``` <br> }<br><br> ```// Function to display a message when the "msgWindow.html" window is closed,``` <br> ```// and the parent window ("window_setTimeout.html") gains focus```<br> ```function showMessage() {``` <br> ```document.write("The notice window opened immediately upon loading,``` <br> ```and it automatically closed after 3 seconds.");```<br> }<br>```</script>```<br>```</head>```<br>```<body onLoad="openMessageWindow()" onFocus="showMessage()">```<br>```</body>```<br><br><br> - There are two functions, openMessageWindow() and showMessage(). The openMessageWindow() function opens a window with the content from "msgWindow.html" using window.open. The showMessage() function is intended to display a message in the parent window ("window_setTimeout.html") when it gains focus.<br><br>The ```<body>``` element has onLoad and onFocus attributes that trigger the openMessageWindow() function to open the message window immediately upon loading the page. Additionally, when the parent window gains focus, the showMessage() function is called to display a message in the document. 	|
| ```</script>``` 	|  	|  	|  	|